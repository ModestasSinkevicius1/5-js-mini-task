const a = 7;
const b = 4;

const c = (a > b) ? true : false;

console.log(c);

const num = 1;

switch (num) {
    case (1):
        console.log('Yes');
        break;

    default:
        console.log('No');
        break;
}